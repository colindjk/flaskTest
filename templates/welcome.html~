      <h1>Welcome to Flask!</h2>
      <br>
      <p>Click <a href="/logout">here</a> to go home.</p>
      {% for message in get_flashed_messages() %}
        {{ message }}
      {% endfor %}
    </div>
  </body>
</html> 
